import { runTrivia } from "../../commands/trivia";
import { IrcHandlerBotRequest, IrcHandlerBot } from "../../lib/irc";

describe("commands/trivia", () => {
  test("runTrivia(...)", async () => {
    // const bot = new IrcHandlerBot()
    // const request = new IrcHandlerBotRequest({
    //   event: "privmsg",
    //   usr: "",
    //   chn: "",
    //   msg: "",
    //   tags: new Map(),
    // }, /-/.exec("-"), bot)

    // await runTrivia(request, questions)
  })
})
